# Server configuration
FASTIFY_PORT=3030

# ============================================================================
# AGENT CONFIGURATION MOVED TO JSON FILES
# ============================================================================
# As of Phase 1.5, agent personality configuration has been moved to JSON files
# in config/agents/ directory. This includes:
#
# - System prompts, persona tags
# - LLM settings (model, temperature, API keys)
# - Memory configuration (embedding models, hotpath settings, thresholds)
#
# To configure agents:
# 1. Copy config/agents/template.json to config/agents/your-agent.json
# 2. Edit the JSON file with your agent settings
# 3. Generate a UUID for the 'id' field (use uuidgen or online generator)
# 4. Agent will be auto-discovered and available via API
#
# For backward compatibility, if no JSON configs exist, the system falls back
# to reading from .env (legacy mode). See template.json for the complete schema.
# ============================================================================

# Postgres persistence (Phase 1.5)
POSTGRES_USER=cerebrobot
POSTGRES_PASSWORD=cerebrobot
POSTGRES_DB=cerebrobot
POSTGRES_PORT=5432
DATABASE_URL="postgresql://cerebrobot:cerebrobot@localhost:5432/cerebrobot"

# LangGraph Postgres checkpoint configuration
# Point to localhost when running the backend directly, or to the docker-compose
# network host `postgres` from inside containers/scripts.
LANGGRAPH_PG_URL="postgresql://cerebrobot:cerebrobot@localhost:5432/cerebrobot"

# Frontend configuration
CLIENT_PORT=5173
VITE_API_BASE="http://localhost:3030"
